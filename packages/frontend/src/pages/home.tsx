import { FunctionalComponent, h } from 'preact';
import { Button } from '@peach/components';

export const Home: FunctionalComponent = () => (
  <div>
    <h1>Peach</h1>
    <Button type="inverted">bla</Button>
  </div>
);
