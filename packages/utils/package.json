{
  "name": "@peach/utils",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "license": "GPL-3.0",
  "scripts": {
    "clean": "rm tsconfig.tsbuildinfo && rm -rf dist",
    "build": "tsc --build",
    "watch": "tsc --build --watch"
  },
  "dependencies": {
    "meow": "^7.0.1",
    "pino": "^6.3.1",
    "pino-pretty": "^4.0.0"
  },
  "devDependencies": {
    "@types/pino": "^6.0.1"
  }
}
