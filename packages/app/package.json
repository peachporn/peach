{
  "name": "@peach/app",
  "version": "0.0.0",
  "main": "index.js",
  "license": "GPL-3.0",
  "scripts": {
    "clean": "rm tsconfig.tsbuildinfo && rm -rf dist",
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "dev": "nodemon -w '../*/src' dist/index.js",
    "dev:debug": "nodemon --inspect -w '../*/src' dist/index.js --debug"
  },
  "dependencies": {
    "@peach/backend": "*",
    "@peach/tasks": "*",
    "@peach/utils": "*",
    "apollo-server-express": "^2.14.1",
    "body-parser": "^1.19.0",
    "cron": "^1.8.2",
    "express": "^4.17.1",
    "fastify-static": "^2.7.0",
    "pino-pretty": "^4.0.0",
    "which": "^2.0.2"
  },
  "devDependencies": {
    "@types/cron": "^1.7.2",
    "@types/pino-http": "^5.0.1",
    "@types/which": "^1.3.2",
    "nodemon": "^2.0.4"
  }
}
