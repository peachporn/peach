export const actressUrl = (actress: { id: number }) => `/actresses/${actress.id}`;
export const movieUrl = (movie: { id: number }) => `/movies/${movie.id}`;
export const genreUrl = (genre: { id: number }) => `/genres/${genre.id}`;
